# Restaurant Chengdu 网站使用指南

> 本指南写给餐厅员工（无需任何技术背景）。
> 网站为纯静态网站：没有数据库、没有后台程序，安全、快速、几乎零维护成本。

---

## 一、网站里有什么

| 文件 / 文件夹 | 作用 |
|---|---|
| `index.html` | 首页（介绍、公告、营业时间、联系方式、地图） |
| `commander.html` | 在线点餐页（菜单、购物车、下单表单） |
| `js/config.js` | ★ **员工常改**：公告、营业时间、特殊闭店日、订单设置 |
| `js/menu-data.js` | ★ **员工常改**：全部菜品（名称、价格、辣度、售完） |
| `images/` | 网站图片；`images/plats/` 放菜品照片 |
| `menus/` | 菜单 PDF（换新菜单时用同名文件覆盖即可） |
| `css/`、其余 `js/` 文件 | 网站样式和程序，**不要修改** |

在自己电脑上预览：双击 `index.html` 即可在浏览器打开（点餐页的全部功能都能本地试用）。

---

## 二、最常用：修改首页公告（临时闭店 / 促销 / 调整营业时间）

1. 右键 `js/config.js` → 打开方式 → **文本编辑 (TextEdit)**；
2. 找到 `announcement:` 一段，只改引号里的文字：

```
announcement: {
  enabled: true,              ← true 显示公告，false 隐藏公告
  style: "banner",            ← "banner" 顶部横幅；"popup" 打开网站弹窗（重要事项用这个）
  type: "warning",            ← "info" 蓝色通知 / "warning" 红色重要 / "promo" 金色促销
  title: "Fermeture exceptionnelle",       ← 标题（法语，给客人看）
  message: "Le restaurant sera fermé du 13 au 14 avril.",  ← 内容
  startDate: "2026-04-01",    ← 从哪天开始显示（留空 "" = 立即）
  endDate: "2026-04-14"       ← 显示到哪天（留空 "" = 一直显示）
},
```

3. 保存（⌘S），刷新网页即可看到。文件里已写好「临时闭店」和「促销」两个现成示例，直接复制替换即可。

**注意**：只改引号里的文字，不要动逗号、引号、大括号。建议改之前先复制一份 `config.js` 备份。

如遇特殊闭店日，同时把日期加入 `specialClosures`（例如 `["2026-04-13", "2026-04-14"]`），这样客人当天就选不到自取时间。

### 大公告板弹窗（打开首页自动弹出的大窗口）

`config.js` 里 `popup:` 一段控制。适合重大促销、周年庆、特殊闭店等需要客人一眼看到的事：

- `enabled: true / false` —— 开关；
- `type` —— `"promo"` 红金喜庆 / `"warning"` 重要通知 / `"info"` 一般通知；
- `icon` —— 顶部大图标，可换 🎉 🏮 ⚠️ 📢 等；
- `lines` —— 正文，一行一段；想**加粗变红**的文字用 `**文字**` 包起来；
- `buttonText` / `buttonLink` —— 底部按钮；促销时链到 `"commander.html"`（去点餐），
  闭店通知就写 `"J'ai compris"` 和 `""`（点了只关闭）；
- `startDate` / `endDate` —— 自动上下线日期。

关闭按钮只是让客人看完后可以先关掉当前这次浏览；只要还在 `enabled: true` 和起止日期范围内，**每次打开或刷新网页都会重新出现**，这样能保证每一位客人都看到重要通知。想让它彻底消失，把 `enabled` 改成 `false`，或者等 `endDate` 那天自动过期。
大公告板和顶部横幅可以同时使用，也可以只开一个。

---

## 二-B、优惠券（改为店内实体券，网上已下架）

网站在线点餐**不再支持**输入优惠码打折——这个功能已经从结账页面拿掉了。
现在的优惠方式是**纸质实体券**：客人首次消费后送一张券，之后单次消费满 150 瑞郎可以使用。
这张券只能在店内由收银人工核销（对券号、划掉/收回），网站和点餐流程不涉及，无需改动任何文件。

---

## 二-C、每周特价栏

菜单最前面有一个金色的 **Promo de la semaine（每周特价）** 分类，在 `menu-data.js` 顶部：

- `price` 填特价、`oldPrice` 填原价（显示删除线），例如原价 20 特价 17；
- 每周更换：改菜名和两个价格即可；
- 想暂停特价栏：把 items 里的菜删光只留 `items: []`，整栏会自动隐藏。

---

## 二-D、主推菜（招牌菜聚光灯）

`config.js` 里 `signatureDish:` 一段。设置后会在**首页英雄区正下方**和**点餐页菜单最上方**各显示一个深红金边的聚光灯大卡片，菜单里对应菜品还会带 ⭐ Signature 角标：

- `dishNo` —— 填菜品编号（如 `"S1"`、`"34"`、`"P1"`），必须是菜单里存在的编号；
- `tagline` —— 一句法语推荐语；
- 点餐页的卡片上有"一键加入购物车"按钮；
- 想换主推菜只需改编号，想隐藏就把 `enabled` 改 `false`。

---

## 三、修改菜单（价格 / 售完 / 上下架 / 加照片）

打开 `js/menu-data.js`，每道菜是一行：

```
{ no: "34", name: "Poulet sauté à la façon Sichuan", nameCn: "四川鸡",
  desc: "Wok épicé", price: 20, spicy: true, photo: "", soldOut: false },
```

- **改价格**：改 `price:` 后面的数字（瑞郎）；
- **临时售完**：把 `soldOut: false` 改成 `true`（显示"Épuisé"且无法下单）；
- **下架**：删除整行（从 `{` 到 `},`）；
- **新增**：复制一行、粘贴、修改文字；
- **加照片**：把照片放进 `images/plats/` 文件夹（建议正方形、小于 500KB），
  然后填 `photo: "文件名.jpg"`。不加照片会自动显示美观的图案占位，完全可以先不放照片。

---

## 四、客人如何下单、餐厅如何收单

客人流程：浏览菜单 → 加购物车 → 填姓名、电话（邮箱选填）→ 选自取日期和时间 → 提交。
**全程无需注册账户，网站也不收任何款项，页面多处明确标注「仅到店付款」。**

自取时间由 `config.js` 自动计算：只显示营业时段内、且距现在至少 30 分钟的时间（15 分钟一档），打烊前 30 分钟停止接单，特殊闭店日自动跳过。这些参数都能在 `config.js` 的 `ordering:` 里调。

### 餐厅怎么收到订单？

**已配置好，正在使用 Formspree 自动转发**：`config.js` 里 `orderEndpoint` 已填好专属地址（`https://formspree.io/f/mkodvvab`）。客人点"确认"订单会**直接发送**，无需客人做任何额外操作。免费版每月 50 单，付费版（约 10 美元/月）无限制。

- **收件邮箱以 Formspree 后台这个表单里设置的为准**，跟登录 Formspree 用的邮箱是两回事——想查看/修改实际收件地址：登录 formspree.io → 打开这个表单 → 找 "Email Notifications" 一栏 → 改成想要的邮箱 → 去那个邮箱里点确认链接激活即可，**不用改网站代码**，`orderEndpoint` 那串网址不变。
- 建议在收银台旁边放一台平板或手机，登录收件邮箱并开启新邮件提醒音，订单一来就能听到。
- ⚠️ 如果 Formspree 服务临时故障或没验证收件邮箱，订单**不会**自动改用邮件方式发送——客人只会看到"发送失败，请致电餐厅"的提示，餐厅这边收不到任何通知。所以务必确认 Formspree 表单里的收件邮箱已经验证生效。

---

## 五、常见问题：域名、服务器、上线

### 1. 没有绑定自有域名，能先开发、测试、试运营吗？

**完全可以，这正是推荐的做法。**
- 开发和测试：直接在电脑上双击打开，或发给朋友试用（见下方"上线"）；
- 上线试运营：用免费托管平台赠送的网址（如 `chengdu-fribourg.netlify.app`），
  功能与正式域名完全一样，可以印在店内海报二维码上先用起来；
- 满意后再绑定正式域名（比如把现有的 `chengdu-suisse.ch` 指过来，或注册新域名，瑞士 .ch 域名约 10–15 瑞郎/年）。绑定过程不影响已建好的内容，网址无缝切换。

### 2. 需要租服务器吗？

**不需要。** 本网站是纯静态网站，推荐用免费静态托管（大品牌、稳定、自带 HTTPS 加密和全球加速）：

| 方案 | 费用 | 适合 |
|---|---|---|
| **Netlify**（推荐） | 免费 | 打开 app.netlify.com/drop，把整个网站文件夹**拖进浏览器**即上线，最适合零基础 |
| Cloudflare Pages / Vercel | 免费 | 同类替代，稍偏技术向 |
| 传统虚拟主机（如 Hostpoint 瑞士） | 约 100+ 瑞郎/年 | 无必要，不推荐 |

对比：你们现有网站用 WordPress（需要主机、要装安全更新、可能被入侵）；这个新网站没有后台程序，**没有可被攻击的入口，也没有主机年费**。

### 3. 上线步骤（用 Netlify，约 15 分钟）

1. 打开 `app.netlify.com` → 免费注册（用餐厅邮箱）；
2. 把 `CCCC` 整个文件夹拖进 Netlify Drop 页面；
3. 得到网址（可在设置里改成好记的名字，如 `chengdu-fribourg.netlify.app`）；
4. 在店里贴出网址/二维码，即可开始接单；
5. 以后每次改了公告或菜单，重新拖一次文件夹即完成更新（约 10 秒）。

### 4. 日常内容管理小结

| 想做的事 | 操作 |
|---|---|
| 发/撤公告 | 改 `js/config.js` → 重新拖上传 |
| 改价格、售完 | 改 `js/menu-data.js` → 重新拖上传 |
| 换菜单 PDF | 覆盖 `menus/` 里的同名文件 → 重新拖上传 |
| 加菜品照片 | 放进 `images/plats/` + 在菜单文件里填文件名 → 重新拖上传 |

**备份**：把整个网站文件夹复制一份存在 U 盘或网盘即可，那就是完整的网站。

---

## 六、下一步可以考虑（可选）

- **绑定正式域名**：试运营满意后，把 `chengdu-suisse.ch` 的 DNS 指向新站（Netlify 后台有向导，或请我协助）；
- **订单短信/WhatsApp 通知**：如果邮件提醒不够及时，可以升级为 WhatsApp 通知方案；
- **多语言**：目前面向客人是法语（菜名附中文），可增加德语或英语版本；
- **菜品拍照**：拍一批真实菜品照片放上去，点餐转化率会明显提高。
